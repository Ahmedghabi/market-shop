import { BoutiqueCentralPage } from '../../screens/public/BoutiqueCentralPage';

export function BoutiqueCentralRoutePage({ title, description }: { title: string; description: string }) {
  return <BoutiqueCentralPage title={title} description={description} />;
}
