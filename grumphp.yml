grumphp:
    tasks:
        composer: null
        phpcs:
            standard: phpcs.xml.dist
        phpstan:
            configuration: phpstan.neon
        phpunit:
            config_file: phpunit.xml.dist
